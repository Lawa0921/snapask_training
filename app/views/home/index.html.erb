  <% if current_user %>
    <%= link_to(t("users.log_out"), destroy_user_session_path, :method => :delete) %> |
    <%= link_to(t("users.edit"), edit_registration_path(:user)) %>
  <% else %>
    <%= link_to(t("users.sign_up"), new_registration_path(:user)) %> |
    <%= link_to(t("users.log_in"), new_session_path(:user)) %>
  <% end %>
