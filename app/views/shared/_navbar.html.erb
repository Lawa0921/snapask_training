<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <%= link_to "SnapaskTraining", root_path, class:"navbar-brand"%>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <% if current_user %>
        <li class="nav-item">
          <%= link_to t("users.log_out"), destroy_user_session_path, method: "DELETE", class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to t("users.edit_account"), edit_registration_path(:user), class: "nav-link" %>
        </li>
        <% if teacher_or_admin? %>
          <li class="nav-item">
            <%= link_to t("courses.list"), courses_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to t("courses.new"), new_course_path, class: "nav-link" %>
          </li>
        <% end %>
        <li class="nav-item ml-auto">
          <%= link_to t("courses.clean_cart"), cart_path, method: "DELETE", class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to cart_path, class: "nav-link" do %>
            <%= t("courses.cart") %> <%= current_cart.courses.count %>
          <% end %>
        </li>
      <% else %>
        <li class="nav-item">
          <%= link_to t("users.sign_up"), new_registration_path(:user), class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to t("users.log_in"), new_session_path(:user), class: "nav-link" %>
        </li>
      <% end %>
    </ul>
  </div>
</nav>

<p class="notice"><%= notice %></p>
<p class="alert"><%= alert %></p>
